import { useEffect } from "react";

const useNotificationWebSocket = () => {
  useEffect(() => {
    // Placeholder - WebSocket connection sẽ được implement sau
    console.log("WebSocket notification listener initialized");
  }, []);
};

export default useNotificationWebSocket;
